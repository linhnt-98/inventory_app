Changes to be made to the UI prototype. as of 3/9/2026

edit the numbers should be option,
make table layout for the activity,
make sure mobile keyboard pops up as numbers for quicker.

Manage tab should also have users, as the manager should be able to add new user to group.

for items, they are first in boxes, everything. Then, an employee might open the box, and take like one stack of cups from the box, for example.

add customizable categories and units, so users can change them and add new ones or remove them.